@model IEnumerable<Hozio.Models.admin>

@{
    ViewBag.Title = "Index";
}

    <h2>Admin Console</h2>

<div class="navbar-collapse collapse">
    <ul class="nav navbar-nav">
        <li>@Html.ActionLink("Admin Console - Edit", "Index1", "admin")</li>
    </ul>
</div>


<table class="table">
    <tr>
        <th>
            Resource
        </th>
        <th>
            Note
        </th>
        <th>

        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                <a href="@item.column02">@Html.DisplayFor(modelItem => item.column01)</a>

            </td>
            <td>
                @Html.DisplayFor(modelItem => item.column03)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.column04)
            </td>
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id=item.adminID }) |
                    @Html.ActionLink("Details", "Details", new { id=item.adminID }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.adminID })*@
            </td>
        </tr>
    }

</table>
